/**
 * Enum representing the possible states of a request.
 * @readonly
 * @enum {string}
 */
export const REQUEST_STATES = {
  IDLE: 'IDLE',
  PENDING: 'PENDING',
  FULFILLED: 'FULFILLED',
  REJECTED: 'REJECTED',
};
